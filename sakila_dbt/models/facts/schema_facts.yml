version: 2

models:
  - name: fact_rental
    description: "Rental fact"
    columns:
      - name: rental_id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null
